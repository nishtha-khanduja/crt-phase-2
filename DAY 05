//Definition for singly-linked list.
 struct ListNode {
      int val;
      ListNode *next;
      ListNode() : val(0), next(nullptr) {}
      ListNode(int x) : val(x), next(nullptr) {}
      ListNode(int x, ListNode *next) : val(x), next(next) {}
  };
 
class Solution {
public:
    ListNode* reverseList(ListNode* head) {
        ListNode* previous= nullptr;
        ListNode* current= head;
        while (current!=nullptr){
            ListNode* next= current-> next;
            current->next= previous;
            previous= current;
            current= next;
        }
        return previous;
    }
};


here, both the next means something else. one represents the address of another node. and another is used to just refer (as a name of a node).
To avoid confusion, lets name the next we created as agla. the another next is the one we defined in struct


class Solution {
public:
    ListNode* reverseList(ListNode* head) {
        ListNode* previous=nullptr;
        ListNode* current=head;
        while(current!=nullptr){
            ListNode* agla=current->next;
            current->next=previous;
            previous=current;
            current=agla;
        }
        return previous;
    }
};
